# Web Proxy with Interception

## Usage
To run the proxy, execute the following command:
```bash
go run .
```
Then configure your browser to use the proxy at `localhost:8888` and open the ui at `localhost:8000`.

